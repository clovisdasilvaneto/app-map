const styles = `
  .app-map-button {
    width: 40px;
    height: 40px;
    background: #fff;
    box-shadow: 1px 1px 3px rgba(0, 0, 0, 0.5);
    border-radius: 50%;
    border: none;
    cursor: pointer;
    font-family: 'Inter', sans-serif;
  }
`;

export default styles;
